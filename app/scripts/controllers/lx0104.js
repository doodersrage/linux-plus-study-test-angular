'use strict';

/**
 * @ngdoc function
 * @name quizApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the quizApp
 */
angular.module('quizApp')
  .controller('Lx0104Ctrl', function ($scope) {
    $scope.questions = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
